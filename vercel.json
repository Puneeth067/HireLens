{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "npm install --legacy-peer-deps && npm run build --workspace=@hirelens/shared-types && npm run build --workspace=@hirelens/config && npm run build --workspace=client",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "client/.next"
}